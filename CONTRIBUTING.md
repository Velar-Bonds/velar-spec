# Contribuir a velar-spec

1. Cambios de enum → actualizar schema JSON + `CHANGELOG.md`.
2. Cambios breaking → etiqueta `spec-breaking` y bump major.
3. Mantener sincronía con `Velar/packages/types`.
